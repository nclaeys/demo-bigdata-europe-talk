
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    default: true
    presets:
      name_prefix: ${bundle.target} # prefix all resource names with dev_ or qa_
    workspace:
      host: https://adb-1366372737668997.17.azuredatabricks.net

  qa:
    mode: production
    presets:
      name_prefix: ${bundle.target} # prefix all resource names with dev_ or qa_
    run_as:
      service_principal_name: '062768dc-f7e0-45d7-8c11-8bec34a5876d'
    workspace:
      host: https://adb-1366372737668997.17.azuredatabricks.net
